general:
  whitelist: true
  pipeline_base_name: websiteM

compile:
  commands:
    - tasks:
      - cmd: docker build -f Dockerfile -t zuul2:$GO_PIPELINE_COUNTER .
      - cmd: docker run --name zuul2_container -d -p 8180:8180 zuul2:$GO_PIPELINE_COUNTER
        build_path: .
